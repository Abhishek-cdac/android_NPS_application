<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          package="com.nectar.nps">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <dist:module dist:instant="true"/>
    <application
            android:name="com.nectarinfotel.utils.NectarApplication"
            android:allowBackup="true"
            android:icon="@drawable/logo"
            android:label="@string/app_name"
            android:roundIcon="@drawable/logo"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".SplashActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".AlarmDetialActivity"/>
        <activity android:name=".LiveAlarmFragment"/>
        <activity android:name=".LiveKPIFragment"/>
        <activity android:name=".MajorAlarmDetialActivity"/>
        <activity android:name=".MinarAlarmDetialActivity"/>
        <activity android:name=".ClearedlAlarmDetialActivity"/>
        <activity android:name=".DashboardAlarmList"/>
    </application>

</manifest>